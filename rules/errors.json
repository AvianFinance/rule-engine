{
    "price-not-met" : ["Price is not enough to do the transaction", "PriceNotMet(address nftAddress, uint256 tokenId, uint256 price)"],
    "nft-not-for-sale" : ["Requested Item is not for sale", "ItemNotForSale(address nftAddress, uint256 tokenId)"],
    "nft-not-listed" : ["Requested NFT is not listed in the marketplace", "NotListed(address nftAddress, uint256 tokenId)"],
    "nft-already-listed" : ["Cannot list already listed NFT", "AlreadyListed(address nftAddress, uint256 tokenId)"],
    "no-proceeds" : ["No proceeds to withdraw", "NoProceeds()"],
    "not-owner" : ["Should be the owner to do the transaction", "NotOwner()"],
    "not-approved" : ["Marketplace is not approved for the task", "NotApprovedForMarketplace()"],
    "price-above-zero" : ["Price must be greater than zero", "PriceMustBeAboveZero()"]
}